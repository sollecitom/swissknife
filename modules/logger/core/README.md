# Logger Core

Custom logging framework: `Logger`, `LoggerFactory`, `LogEntry`, `LoggingLevel`, `LoggingContext` with coroutine-aware MDC support, configurable appenders (PrintStream, functional), log entry formatting and filtering, and a trie-based longest-prefix-match level enabler.
