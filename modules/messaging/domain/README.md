# Messaging Domain

Technology-agnostic messaging abstractions: `Message`, `ReceivedMessage`, `OutboundMessage`, `MessageProducer`, `MessageConsumer`, `MessagePublisher`, `MessageConnector`, `MessageSource`, `MessageConverter`, topic modeling (with tenant isolation), partition assignment tracking, and event processing utilities.
