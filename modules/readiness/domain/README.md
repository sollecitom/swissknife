# Readiness Domain

Readiness check abstractions: `ReadinessAware` interface with coroutine-based health checks, `ReadinessCheckResult` (Passed/Failed), `RequiresConnection`, and `ClientAndCheck` for pairing clients with their readiness checks.
