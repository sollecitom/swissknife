# Avro Schema Repository Domain

Avro schema repository abstractions: `AvroSchemaRepository` interface for schema lookup, `AvroSchemaLocator` and `AvroSchemaName` for schema identification, with a local-resources-based implementation.
