# NATS Client

NATS messaging client: `NatsPublisher` for publishing messages and `NatsConsumer` for consuming messages as Kotlin coroutine flows, with adapter implementations wrapping the jnats library.
