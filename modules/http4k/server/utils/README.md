# Http4k Server Utils

`SuspendingHttpHandler` typealias and extensions for bridging Kotlin coroutine suspend functions to Http4k's synchronous `HttpHandler`, enabling coroutine-based HTTP request handling.
