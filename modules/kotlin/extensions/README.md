# Kotlin Extensions

Pure Kotlin extension functions for collections, coroutines, flows, byte arrays, Base64 encoding, numbers (BigDecimal, Long, Double), strings (random generation, character groups), time/duration, optionals, CompletableFuture, throwables, virtual threads, and runtime utilities.
