# Logging Standard Configuration

Standard logging configuration utilities: `LogFormat` (text/JSON), `LoggingCustomizer`, `StandardLoggingConfiguration`, and `configureLogging` function for setting up the logging framework with consistent defaults.
