# Core Domain

Foundational domain types used across all modules: identity (Id, ULID, KSUID, UUID), currency modeling, email addresses, networking (Port, IpAddress), text (Name, CodeName), lifecycle interfaces (Startable, Stoppable), versioning, and common traits (Identifiable, Timestamped, Contextual, StringSerializable).
