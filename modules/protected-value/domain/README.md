# Protected Value Domain

Encrypted value protection framework: `ProtectedValue` (with accessible variant for decryption), `ProtectedValueFactory` for encrypting arbitrary values, and typed factory wrappers supporting serialization-aware protection with access-controlled decryption.
