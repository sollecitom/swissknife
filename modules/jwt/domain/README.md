# JWT Domain

JWT domain types: `JWT`, `JwtProcessor` (with configuration), `JwtIssuer`, `JwtParty`, `JwtPartyInfo`, `JwtAudience`, `StringOrURI`, RSA algorithm variants, and content encryption algorithm definitions.
