# JWT Jose4j Processor

Jose4j-based JWT processor implementation: `JoseJwtProcessor` for token verification/decryption, `JoseJwtAdapter` for JWT parsing, and factory functions for creating audience-specific processors.
