# Correlation Core Domain

Invocation context and correlation model: `InvocationContext` carrying trace, access, and toggle information through call chains. Includes actor modeling (authenticated/direct/on-behalf), tenant isolation, authorization principals, origin tracking, access scopes, toggle values, and invocation trace with action/invocation IDs.
