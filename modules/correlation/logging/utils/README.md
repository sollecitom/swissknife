# Correlation Logging Utils

Utilities for propagating invocation context into logging MDC: `InvocationContext.toLoggingContext()` and `withLoggingContext` for coroutine-scoped correlation-aware logging.
