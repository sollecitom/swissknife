# Configuration Utils

Configuration loading utilities built on Http4k Environment: `StandardEnvironment` cascading resolution (JVM properties, env vars, YAML files), `ConfigurationSection` base class for typed config sections, and standard property keys (random seed, instance node/group names).
