# Web API Utils

HTTP API utilities built on Http4k: `HttpApi`, `HttpDrivingAdapter`, health endpoint, Prometheus metrics, invocation context filter, content negotiation, error handling, request extensions, and standard HTTP header name definitions.
