# Service Domain

Service identity and lifecycle: `ServiceInfo` (instance ID + name), `ServiceModule` interface combining Startable/Stoppable with a name, and `ServiceModuleWithHttpDrivingAdapter` for modules exposing HTTP endpoints.
