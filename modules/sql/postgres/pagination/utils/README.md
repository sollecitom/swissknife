# SQL PostgreSQL Pagination Utils

PostgreSQL-specific pagination utilities: `Entity` wrapper with primary key and `SqlEntityFilter` for building paginated SQL queries with sorting and filtering against PostgreSQL using R2DBC.
