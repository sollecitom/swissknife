# Compliance Checker Domain

Generic compliance checking framework: `ComplianceChecker`, `ComplianceRule`, `ComplianceRuleSet`, `ComplianceCheckResult`, and `RulesBasedComplianceChecker` for validating any target against a set of configurable rules.
